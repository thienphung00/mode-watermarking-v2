# Secrets for sensitive configuration
# NOTE: In production, use external secret management (GCP Secret Manager, Vault, etc.)
# This file is for reference only - DO NOT commit real secrets
apiVersion: v1
kind: Secret
metadata:
  name: watermarking-secrets
  namespace: watermarking
  labels:
    app.kubernetes.io/name: watermarking
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Master encryption key for key storage
  # REPLACE WITH ACTUAL VALUE - DO NOT COMMIT
  ENCRYPTION_KEY: "replace-with-actual-encryption-key"
  
  # Database URL (if using PostgreSQL)
  # DATABASE_URL: "postgresql://user:pass@host:5432/watermarking"
  
  # Worker authentication secret
  WORKER_AUTH_SECRET: "replace-with-worker-auth-secret"
---
# External Secret (for use with External Secrets Operator)
# Uncomment and configure for GCP Secret Manager integration
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: watermarking-external-secrets
#   namespace: watermarking
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: gcp-secret-store
#   target:
#     name: watermarking-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: ENCRYPTION_KEY
#       remoteRef:
#         key: watermarking-encryption-key
#     - secretKey: WORKER_AUTH_SECRET
#       remoteRef:
#         key: watermarking-worker-auth

